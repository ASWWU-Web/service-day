<!-- Put all of the header/general layout code here -->
<!-- Index.html is auto-generated, so this file acts as THE main file -->

<nav class="navbar navbar-light bg-faded">
  <div class="container-fluid">
    {{link-to "Service Day Signups" "home" class="navbar-brand"}}
    <ul class="nav navbar-nav pull-xs-right">
      {{#if session.isAuthenticated}}
        {{#if isAdmin}}
          <li class="nav-item">
            {{link-to "Admin" "admin" class="btn btn-danger-outline"}}
          </li>
        {{/if}}
        <li class="nav-item">
          <button class="btn btn-primary-outline" {{action "signOut"}}>Log Out</button>
        </li>
      {{/if}}
    </ul>
  </div>
</nav>
<div class="container">
  <br><br>
  {{outlet}}
</div>
<!-- Place footer and whatever would go after the body here -->
