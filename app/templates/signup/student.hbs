<div class="row">
  {{#each model.student as |stu|}}
      <h2>Hey there {{stu.name}}</h2><br>
      <p>{{instructions.text}}</p>
      <h5> Tell us a bit about yourself.</h5>
      <div class="row">
        <div class="col-xs-12 col-md-6">Name: {{input type='text' value=stu.name class='form-control'}}</div>
        <div class="col-xs-12 col-md-6">Phone:
          {{input type='phone' value=stu.phone class='form-control'}}
        </div>
        <div class="col-xs-12 col-md-6">T-shirt size:
          {{input type='text' value=stu.tShirt class='form-control'}}
        </div>
        <div class="col-xs-12 col-md-6">
          <br>
          School you attend:
          <br>
          {{radio-button value='WWU' checked=stu.school}} Walla Walla University<br>
          {{radio-button value='WWCC' checked=stu.school}} Walla Walla Community College<br>
          {{radio-button value='Whitman' checked=stu.school}} Whitman College<br>
          <br>
        </div>
      </div>
      <p>Would you like to {{#link-to 'signup.facilitator'}}be a facilitator{{/link-to}} for service day?</p>
      <p id='success-text' class="text-success"></p>
      <button class="btn btn-success" {{action 'save' stu}}>Save</button>
      <br><br>
    <h5> Click a project to sign up for it. </h5><br>
    {{#unless stu.projectID}}
      <p class="text-danger"> You aren't signed up for a project.</p>
    {{/unless}}
  {{/each}}
  <table class="table">
    <thead>
      <tr>
        <th>Project</th>
        <th>Address</th>
        <th>Description</th>
        <th>Instructions</th>
        <th>Volunteers</th>
      </tr>
    </thead>
    <tbody>
      {{#each model.student as |stu|}}
        {{#each model.organizations as |org|}}
          {{#if (eq stu.projectID org.id)}}
            <tr {{action "update" org stu}} class="bg-primary">
              <td>{{org.name}}</td>
              <td>{{org.address}}</td>
              <td>{{org.description}}</td>
              <td>{{org.special_instructions}}</td>
              <td>({{org.count}}/{{org.maximum_count}})</td>
            </tr>
          {{else if (gte org.count org.maximum_count)}}

          {{else}}
            <tr {{action "update" org stu}}>
              <td>{{org.name}}</td>
              <td>{{org.address}}</td>
              <td>{{org.description}}</td>
              <td>{{org.special_instructions}}</td>
              <td>({{org.count}}/{{org.maximum_count}})</td>
            </tr>
          {{/if}}
        {{/each}}
      {{/each}}
    </tbody>
  </table>
</div>
